{
  "name": "sgi-os-portal",
  "version": "0.2.0",
  "private": true,
  "description": "Portal SGI operacional com servidor local e persistência JSON.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  }
}
